# Dipendenze per il solo servizio web (api.py), separate da requirements.txt
# che include anche playwright necessario allo scraper. Lo scraper gira in
# GitHub Actions (vedi .github/workflows/scrape-weekly.yml), non sul servizio
# di deploy: qui serve solo il minimo per rispondere alle richieste, così la
# build resta leggera e veloce sui piani gratuiti.
fastapi>=0.110
uvicorn>=0.29
stripe>=10.0
requests>=2.31
pyyaml>=6.0
